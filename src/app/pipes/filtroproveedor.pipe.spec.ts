import { FiltroproveedorPipe } from './filtroproveedor.pipe';

describe('FiltroproveedorPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroproveedorPipe();
    expect(pipe).toBeTruthy();
  });
});
