import { FiltroclientePipe } from './filtrocliente.pipe';

describe('FiltroclientePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroclientePipe();
    expect(pipe).toBeTruthy();
  });
});
