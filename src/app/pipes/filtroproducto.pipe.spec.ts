import { FiltroproductoPipe } from './filtroproducto.pipe';

describe('FiltroproductoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroproductoPipe();
    expect(pipe).toBeTruthy();
  });
});
